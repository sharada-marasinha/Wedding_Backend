spring:
  mail:
    # ===============================================================
    # GMAIL SMTP SERVER SETTINGS
    # ===============================================================
    host: smtp.gmail.com
    port: 587
    username: sharadamarasinha@gmail.com
    # Your 16-digit App Password from Google
    password: xsqx jkrf heue bwnx

    # ===============================================================
    # JAVA MAIL SMTP PROPERTIES
    # ===============================================================
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          # Optional: Add connection timeouts (in milliseconds)
          # connectiontimeout: 5000
          # timeout: 5000
          # writetimeout: 5000
  datasource:
    url: jdbc:mysql://localhost:3306/wedding_db?createDatabaseIfNotExist=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          lob:
            non_contextual_creation: true
  sql:
    init:
      mode: always
  profiles:
    active: dev


app:
  jwt-secret: '5465sdfsdf56sdfsdsfsdf4sdfsdfsd4sdf4sdf4seyours512vbitksecretnkeyvmustsbepatvleastk32pcharactersqlong'
  jwt-expiration-milliseconds: 86400000 # 24 hours




#&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true


# ===============================================================
# GMAIL SMTP SERVER SETTINGS
# ===============================================================

# SMTP server for sending mail
#  spring.mail.host=smtp.gmail.com

  # Port for TLS/STARTTLS
#  spring.mail.port=587

  # Your full Gmail address
#  spring.mail.username=sharadamarasinha@gmail.com

  # Your 16-digit App Password from Google
#  spring.mail.password=xsqx jkrf heue bwnx

  # ===============================================================
  # JAVA MAIL SMTP PROPERTIES
  # ===============================================================
#  spring.mail.properties.mail.smtp.auth=true
#  spring.mail.properties.mail.smtp.starttls.enable=true

# Optional: Add a connection timeout (e.g., 5 seconds)
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.properties.mail.smtp.writetimeout=5000





